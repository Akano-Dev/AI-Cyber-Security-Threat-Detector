# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Virtual environments
venv/
.venv/
env/

# ── Node / frontend build output ────────────────────────────────────────────
node_modules/
dist/

# ── Secrets & local environment ─────────────────────────────────────────────
# .env.example is the tracked template; the real .env stays local only.
.env
.env.local

# ── Local runtime artifacts (regenerated by the app / training) ─────────────
*.db
*.sqlite3
*.pkl
# Generated multi-class dataset (rebuild with build_multiclass_dataset.py).
# metrics_multiclass.json + feature_importance.json stay tracked as evidence.
data/multiclass_dataset.csv
backend/data/multiclass_dataset.csv

# ── OS / editor cruft ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
