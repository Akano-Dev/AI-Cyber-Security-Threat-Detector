# AI Cyber Security Threat Detector (ACSTD)

## Run the backend

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

Runs on http://localhost:8000

## Run the frontend

```bash
cd frontend
npm install
npm run dev
```

Runs on http://localhost:5173
