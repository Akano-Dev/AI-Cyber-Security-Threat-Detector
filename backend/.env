# The real website ACSTD should protect.
# Example: TARGET_URL=https://example.com
# Demo site runs on port 8080 — change this to protect any real site
TARGET_URL=http://localhost:8090

# Rate limiting — auto-block an IP that sends more than RATE_LIMIT requests within RATE_WINDOW seconds
RATE_LIMIT=20
RATE_WINDOW=60
